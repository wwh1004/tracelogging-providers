ProviderNames:
	Microsoft.Windows.Sense.SenseCm
	Microsoft.Windows.Sense.Client



**** Events *****
RegistryUtils(
	UINT32 errorCode,
	ANSISTRING message,
	UNICODESTRING valueName
	);

RegistryUtils(
	UINT32 errorCode,
	ANSISTRING message,
	UNICODESTRING valueName
	);

RegistryUtils(
	UINT32 errorCode,
	ANSISTRING message,
	UNICODESTRING subkey
	);

RegistryUtils(
	UINT32 errorCode,
	ANSISTRING message,
	UNICODESTRING valueName
	);

RegistryUtils(
	UINT32 errorCode,
	ANSISTRING message,
	UNICODESTRING subkey
	);

RegistryUtils(
	UINT32 errorCode,
	ANSISTRING message,
	UNICODESTRING valueName
	);

RegistryUtils(
	UINT32 errorCode,
	ANSISTRING message,
	UNICODESTRING subkey
	);

RegistryUtils(
	UINT32 errorCode,
	ANSISTRING message,
	UNICODESTRING valueName
	);

RegistryUtils(
	UINT32 errorCode,
	ANSISTRING message,
	UNICODESTRING subkey
	);

RegistryUtils(
	UINT32 errorCode,
	ANSISTRING message,
	UNICODESTRING valueName
	);

RegistryUtils(
	UNICODESTRING message,
	UNICODESTRING subkey
	);

RegistryUtils(
	UNICODESTRING message,
	UNICODESTRING subkey
	);

RegistryUtils(
	UNICODESTRING message,
	UNICODESTRING subkey
	);

RegistryUtils(
	UINT32 errorCode,
	ANSISTRING message,
	UNICODESTRING subkey
	);

RegistryUtils(
	ANSISTRING message,
	UNICODESTRING subkey
	);

RegistryUtils(
	UINT32 errorCode,
	ANSISTRING message,
	UNICODESTRING subkey
	);

RegistryUtils(
	UINT32 errorCode,
	ANSISTRING message,
	UNICODESTRING subkey,
	UNICODESTRING valueName
	);

RegistryUtils(
	UINT32 errorCode,
	ANSISTRING message,
	UNICODESTRING subkey
	);

RegistryUtils(
	UINT32 errorCode,
	ANSISTRING message,
	UNICODESTRING subkey,
	UNICODESTRING valueName
	);

RegistryUtils(
	UNICODESTRING message,
	UNICODESTRING privilege
	);

RegistryUtils(
	UINT32 errorCode,
	UNICODESTRING message,
	UNICODESTRING privilege
	);

RegistryUtils(
	UINT32 errorCode,
	UNICODESTRING message,
	UNICODESTRING privilege
	);

RegistryUtils(
	UINT32 errorCode,
	UNICODESTRING message,
	UNICODESTRING privilege
	);

RegistryUtils(
	UINT32 errorCode,
	UNICODESTRING message,
	UNICODESTRING privilege
	);

RegistryUtils(
	UNICODESTRING message,
	UNICODESTRING privilege
	);

SenseHttpClient(
	ANSISTRING message,
	BOOLEAN status
	);

SenseHttpClient(
	UINT32 errorCode,
	ANSISTRING message
	);

SenseHttpClient(
	ANSISTRING message,
	ANSISTRING baseUrl
	);

SenseHttpClient(
	ANSISTRING message,
	ANSISTRING m_baseUrl
	);

SenseHttpClient(
	ANSISTRING message,
	ANSISTRING Parameter name,
	ANSISTRING Parameter value
	);

SenseHttpClient(
	UINT32 errorCode,
	ANSISTRING message
	);

SenseHttpClient(
	ANSISTRING message,
	UINT32 copiedBytes,
	ANSISTRING data
	);

SenseHttpClient(
	ANSISTRING message
	);

SenseHttpClient(
	ANSISTRING message
	);

SenseHttpClient(
	ANSISTRING message
	);

SenseHttpClient(
	ANSISTRING message
	);

SenseHttpClient(
	ANSISTRING message
	);

SenseHttpClient(
	ANSISTRING message
	);

SenseHttpClient(
	ANSISTRING message
	);

SenseHttpClient(
	ANSISTRING message
	);

SenseHttpClient(
	ANSISTRING message
	);

SenseHttpClient(
	ANSISTRING message
	);

SenseHttpClient(
	ANSISTRING message
	);

SenseHttpClient(
	ANSISTRING message
	);

SenseHttpClient(
	ANSISTRING message
	);

SenseHttpClient(
	ANSISTRING message
	);

SenseHttpClient(
	ANSISTRING message
	);

SenseHttpClient(
	ANSISTRING message
	);

SenseHttpClient(
	ANSISTRING message
	);

SenseHttpClient(
	ANSISTRING message
	);

SenseHttpClient(
	ANSISTRING message
	);

SenseHttpClient(
	ANSISTRING message
	);

SenseHttpClient(
	ANSISTRING message
	);

SenseHttpClient(
	ANSISTRING message
	);

SenseHttpClient(
	ANSISTRING message
	);

SenseHttpClient(
	ANSISTRING message
	);

SenseHttpClient(
	ANSISTRING message
	);

SenseHttpClient(
	ANSISTRING message
	);

SenseHttpClient(
	ANSISTRING message
	);

SenseHttpClient(
	ANSISTRING message
	);

SenseHttpClient(
	ANSISTRING message
	);

SenseHttpClient(
	ANSISTRING message
	);

SenseHttpClient(
	ANSISTRING message
	);

SenseHttpClient(
	ANSISTRING message
	);

SenseHttpClient(
	ANSISTRING message
	);

SenseHttpClient(
	ANSISTRING message
	);

SenseHttpClient(
	ANSISTRING message
	);

SenseHttpClient(
	ANSISTRING message
	);

SenseHttpClient(
	ANSISTRING message
	);

SenseHttpClient(
	ANSISTRING message,
	ANSISTRING header
	);

SenseHttpClient(
	ANSISTRING message
	);

SenseHttpClient(
	ANSISTRING message,
	ANSISTRING url,
	ANSISTRING Headers,
	ANSISTRING proxyAddress,
	UINT16 proxyPort
	);

SenseHttpClient(
	ANSISTRING message
	);

SenseHttpClient(
	ANSISTRING message,
	ANSISTRING url,
	ANSISTRING Headers,
	INT32 code,
	ANSISTRING error string,
	INT32 httpcode,
	ANSISTRING proxyAddress,
	UINT16 proxyPort
	);

SenseHttpClient(
	ANSISTRING message
	);

SenseHttpClient(
	UINT32 errorCode,
	ANSISTRING message,
	UINT32 res
	);

SenseHttpClient(
	ANSISTRING message,
	ANSISTRING version,
	ANSISTRING sslVersion,
	ANSISTRING libzVersion
	);

SenseHttpClient(
	ANSISTRING message
	);

SenseHttpClient(
	ANSISTRING message
	);

SenseHttpClient(
	ANSISTRING message,
	ANSISTRING url
	);

SenseHttpClient(
	ANSISTRING message
	);

User(
	INT32 errorCode,
	ANSISTRING message
	);

SenseHttpClient(
	ANSISTRING message,
	ANSISTRING attemptNumber,
	ANSISTRING proxyAddress,
	ANSISTRING proxyPort
	);

SenseHttpClient(
	ANSISTRING message,
	ANSISTRING attemptNumber
	);

User(
	INT32 errorCode,
	ANSISTRING message
	);

SenseHttpClient(
	UINT32 errorCode,
	ANSISTRING message,
	BOOLEAN ssl_context_ptr,
	BOOLEAN user_data_ptr
	);

SenseHttpClient(
	UINT32 errorCode,
	ANSISTRING message,
	ANSISTRING exception
	);

SenseHttpClient(
	UINT32 errorCode,
	ANSISTRING message
	);

ProcessUtils(
	UNICODESTRING message
	);

ProcessUtils(
	UNICODESTRING message
	);

ProcessUtils(
	UNICODESTRING message
	);

ProcessUtils(
	UNICODESTRING message
	);

ProcessUtils(
	UNICODESTRING message
	);

ProcessUtils(
	UNICODESTRING message
	);

ProcessUtils(
	UNICODESTRING message
	);

ProcessUtils(
	ANSISTRING message
	);

ProcessUtils(
	ANSISTRING message
	);

ProcessUtils(
	UNICODESTRING message,
	UINT32 processId
	);

ProcessUtils(
	UNICODESTRING message,
	UINT32 processId,
	INT32 errorCode
	);

ProcessUtils(
	UNICODESTRING message,
	UINT32 processId,
	INT32 errorCode
	);

ProcessUtils(
	UNICODESTRING message
	);

ProcessUtils(
	UNICODESTRING message
	);

ProcessUtils(
	UNICODESTRING message
	);

ProcessUtils(
	UNICODESTRING message
	);

ProcessUtils(
	UNICODESTRING message
	);

ProcessUtils(
	UNICODESTRING message
	);

ProcessUtils(
	UNICODESTRING message
	);

ProcessUtils(
	UINT32 errorCode,
	ANSISTRING message
	);

SenseCM(
	ANSISTRING message
	);

SenseCM(
	INT32 errorCode,
	ANSISTRING message
	);

SenseCM(
	ANSISTRING message
	);

SenseCM(
	INT32 errorCode,
	UNICODESTRING message,
	UNICODESTRING port,
	ANSISTRING Exception message
	);

SenseCommon(
	ANSISTRING message,
	UINT32 ERR_get_error
	);

SenseCommon(
	UINT32 errorCode,
	ANSISTRING message,
	UINT32 error
	);

SenseCommon(
	UINT32 errorCode,
	ANSISTRING message,
	UINT32 error
	);

SenseCommon(
	UINT32 errorCode,
	ANSISTRING message,
	UINT32 error
	);

SenseCommon(
	UINT32 errorCode,
	ANSISTRING message,
	UINT32 error
	);

SenseCommon(
	UINT32 errorCode,
	ANSISTRING message,
	UINT32 error
	);

SenseCommon(
	UINT32 errorCode,
	ANSISTRING message,
	UINT32 error
	);

SenseCM(
	ANSISTRING message,
	ANSISTRING params
	);

CheckinScriptResults(
	ANSISTRING message,
	ANSISTRING params
	);

SenseCM(
	ANSISTRING message,
	ANSISTRING params
	);

SenseCM(
	ANSISTRING message,
	ANSISTRING eventName
	);

SenseCM(
	ANSISTRING message,
	ANSISTRING params
	);

CheckinScriptResults(
	ANSISTRING message,
	ANSISTRING params
	);

SenseCM(
	ANSISTRING message,
	ANSISTRING params
	);

SenseCM(
	ANSISTRING message,
	ANSISTRING eventName
	);

SenseCM(
	ANSISTRING message,
	ANSISTRING params
	);

CheckinScriptResults(
	ANSISTRING message,
	ANSISTRING params
	);

SenseCM(
	ANSISTRING message,
	ANSISTRING params
	);

SenseCM(
	ANSISTRING message,
	ANSISTRING eventName
	);

SenseCM(
	ANSISTRING message,
	ANSISTRING params
	);

CheckinScriptResults(
	ANSISTRING message,
	ANSISTRING params
	);

SenseCM(
	ANSISTRING message,
	ANSISTRING params
	);

SenseCM(
	ANSISTRING message,
	ANSISTRING eventName
	);

